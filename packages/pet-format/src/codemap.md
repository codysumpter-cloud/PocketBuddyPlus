# packages/pet-format/src/

- `index.ts`: manifest types, validation, stable animation-ID normalization, selectable-animation filtering, direction lookup, and semantic fallback resolution.
- `check-pet-format.ts`: executable contract coverage for validation, path safety, incomplete-animation filtering, original-name preservation, and fallback behavior.
